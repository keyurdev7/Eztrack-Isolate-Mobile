import 'package:flutter/material.dart';

// =============================================================================
// PRIMARY COLORS
// =============================================================================
const primary = Color(0xFFF54E00); // Orange accent color
 // Orange accent color
const secondary = Color(0xFFFB3434);

// =============================================================================
// TEXT COLORS
// =============================================================================
const bodyTextColor = Color(0xFF2B323B); // Body text color
const appBarTextColor = Color(0xFF0B0D0F); // App bar text color
const bottomBarTextColor = Color(0xFF4F5E6E); // Bottom bar text color
const mainTextColor = Color(0xFF000000); // Main text color
const lightTextColor = Color(0xFF8E8E93); // Light gray text

// =============================================================================
// BACKGROUND COLORS
// =============================================================================
const appBgColor = Color(0xFFF2F2F7); // Light gray background
const cardColor = Color(0xFFFFFFFF); // White cards
const darker = Color(0xFF2C2C2E); // Dark gray background
const lightBackground = Color(0xFFF2F2F7); // Light gray background

// =============================================================================
// UI COMPONENT COLORS
// =============================================================================
const bottomBarColor = Color(0xFFFFFFFF); // White bottom bar
const textBoxColor = Color(0xFFE5E5EA);
const shadowColor = Colors.black26;
const inActiveColor = Color(0xFF8E8E93); // Gray for inactive items

// =============================================================================
// ACCENT COLORS
// =============================================================================
const yellow = Color(0xFFFFCB66);
const green = Color(0xFFB2E1B5);
const pink = Color(0xFFF5BDE8);
const purple = Color(0xFFD9BCFF);
const red = Color(0xFFFF6B35); // Orange for delete/logout actions (matching primary)
const orange = Color(0xFFFF6B35); // Primary orange
const sky = Color(0xFFABDEE6);
const blue = Color(0xFF509BE4);

// =============================================================================
// UTILITY COLORS
// =============================================================================
const white = Color(0xFFFFFFFF); // White
const lightGray = Color(0xFF8E8E93); // Gray text
const darkGray = Color(0xFF2C2C2E); // Dark gray

// =============================================================================
// COLOR LISTS
// =============================================================================
const listColors = [green, primary, pink, red, yellow, orange, sky, blue, purple, yellow];